# Vite React Example

Simple browser example for `@kittentts/web`.

## Run

From the SDK root:

```bash
npm run build
cd examples/vite-react
npm install
npm run dev
```

Open the Vite URL, initialize the SDK, then generate or speak.
