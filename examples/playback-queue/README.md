# KittenTTS Playback Queue Example

This example shows the Web SDK `createPlaybackQueue()` API with three editable
clips. Each clip is generated and played only after the previous clip finishes.

Run it from the repository root:

```bash
npm run example:queue
```

Then open `http://localhost:5175/playback-queue/`.
