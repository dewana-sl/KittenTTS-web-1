# Vite React Word Timings Example

Read-aloud highlighting example for `@kittentts/web`.

## Run

From the SDK root:

```bash
npm run build
cd examples/vite-react-word-timings
npm install
npm run dev
```

Open the Vite URL, initialize the SDK, generate timings, then play with
highlighting.
